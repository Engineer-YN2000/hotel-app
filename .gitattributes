# -------------------------------------------------------------------
# Git属性設定
# 改行コードを全環境で "LF" に統一し、CRLFへの自動変換を無効化する
# -------------------------------------------------------------------

# まず、すべてのファイルを「テキストファイル」として扱い、
# 改行コードを自動で "LF" に正規化する。
* text=auto eol=lf

# -------------------------------------------------------------------
# バイナリファイル (改行コード変換の対象外)
# -------------------------------------------------------------------

# 一般的な画像フォーマット
*.png      binary
*.jpg      binary
*.jpeg     binary
*.gif      binary
*.ico      binary
*.svg      binary

# フォント
*.eot      binary
*.ttf      binary
*.woff     binary
*.woff2    binary

# その他
*.pdf      binary
*.zip      binary
*.jar      binary
